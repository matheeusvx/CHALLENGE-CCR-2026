# Infraestrutura

Esta pasta reserva configuracoes de infraestrutura que serao adicionadas em etapas
posteriores. Nesta fundacao, a execucao local esta definida somente no
`docker-compose.yml` da raiz, com os servicos `api` e `web`.

Nao ha banco de dados, fila, worker, cache ou servidor de tiles nesta etapa.
